---
title: YH-AI PPT 生产环境部署指南
code_version: v1.2.3
last_updated: 2026-02-16
author: 运维团队
reviewer: 技术负责人
status: published
modules: [部署, 运维]
tags: [部署, 生产环境, Docker, CI/CD]
---

# 🚀 YH-AI PPT 生产环境部署指南

> **文档状态**: ✅ 已发布  
> **最后同步代码版本**: v1.2.3 (2026-02-16)  

## 概述

本文档描述 YH-AI PPT 生产环境的部署流程，包含 Docker 部署、Nginx 配置、SSL 证书、以及 CI/CD 流水线设置。

## 部署架构

```
┌─────────────────────────────────────────────────────────────┐
│                        生产环境架构                          │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│   用户请求                                                    │
│       ↓                                                      │
│   [Nginx] 反向代理 + 负载均衡 + SSL                          │
│       ↓                                                      │
│   ┌──────────────┐                                          │
│   │  Frontend    │  React SPA (Docker)                      │
│   │  Port: 1000  │                                          │
│   └──────────────┘                                          │
│       ↓                                                      │
│   [Nginx API Proxy] /api → backend                          │
│       ↓                                                      │
│   ┌──────────────┐                                          │
│   │  Backend     │  Express API (Docker)                    │
│   │  Port: 1111  │                                          │
│   └──────────────┘                                          │
│       ↓                                                      │
│   [SQLite Database] + [File Storage]                         │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

## 快速部署

### 1. 使用 Docker Compose（推荐）

```bash
# 1. 克隆代码
git clone https://github.com/your-org/yh-ai-ppt.git
cd yh-ai-ppt

# 2. 配置环境变量
cp .env.production.example .env
vim .env  # 编辑生产环境配置

# 3. 启动服务
docker-compose up -d

# 4. 查看状态
docker-compose ps
```

### 2. 手动部署

#### 2.1 服务器准备

**系统要求**:
- Ubuntu 22.04 LTS / CentOS 8+
- 4核 CPU / 8GB RAM / 50GB SSD
- 域名已解析到服务器

#### 2.2 安装 Docker

```bash
# Ubuntu
curl -fsSL https://get.docker.com | sh
sudo systemctl enable docker
sudo systemctl start docker

# 安装 docker-compose
sudo curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
sudo chmod +x /usr/local/bin/docker-compose
```

#### 2.3 配置 Nginx

```nginx
# /etc/nginx/sites-available/yh-ai-ppt

server {
    listen 80;
    server_name your-domain.com;
    
    # 重定向到 HTTPS
    return 301 https://$server_name$request_uri;
}

server {
    listen 443 ssl http2;
    server_name your-domain.com;
    
    # SSL 证书
    ssl_certificate /path/to/fullchain.pem;
    ssl_certificate_key /path/to/privkey.pem;
    
    # 前端静态文件
    location / {
        root /var/www/yh-ai-ppt/dist;
        try_files $uri $uri/ /index.html;
        
        # 缓存策略
        location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg)$ {
            expires 1y;
            add_header Cache-Control "public, immutable";
        }
    }
    
    # API 代理
    location /api/ {
        proxy_pass http://localhost:1111/;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        
        # 超时设置
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }
    
    # 上传文件
    location /uploads/ {
        alias /var/www/yh-ai-ppt/uploads/;
        expires 30d;
        add_header Cache-Control "public";
    }
}
```

启用配置:
```bash
sudo ln -s /etc/nginx/sites-available/yh-ai-ppt /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl reload nginx
```

## Docker 部署

### Dockerfile - 前端

```dockerfile
# Dockerfile.frontend
FROM node:22-alpine AS builder

WORKDIR /app

# 安装依赖
COPY package*.json ./
RUN npm ci --only=production

# 构建
COPY . .
RUN npm run build

# 生产镜像
FROM nginx:alpine
COPY --from=builder /app/dist /usr/share/nginx/html
COPY nginx.conf /etc/nginx/conf.d/default.conf

EXPOSE 80
```

### Dockerfile - 后端

```dockerfile
# Dockerfile.backend
FROM node:22-alpine

WORKDIR /app

# 安装依赖
COPY server/package*.json ./
RUN npm ci --only=production

# 复制代码
COPY server/dist ./dist
COPY server/prisma ./prisma

# 生成 Prisma Client
RUN npx prisma generate

# 暴露端口
EXPOSE 1111

# 健康检查
HEALTHCHECK --interval=30s --timeout=3s --start-period=5s --retries=3 \
    CMD curl -f http://localhost:1111/api/health || exit 1

# 启动
CMD ["node", "dist/app.js"]
```

### Docker Compose 配置

```yaml
# docker-compose.yml
version: '3.8'

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile.frontend
    ports:
      - "1000:80"
    depends_on:
      - backend
    restart: unless-stopped
    networks:
      - yh-ai-ppt

  backend:
    build:
      context: .
      dockerfile: Dockerfile.backend
    ports:
      - "1111:1111"
    environment:
      - NODE_ENV=production
      - DATABASE_URL=file:./prisma/prod.db
      - JWT_SECRET=${JWT_SECRET}
      - GEMINI_API_KEY=${GEMINI_API_KEY}
    volumes:
      - ./data:/app/prisma
      - ./uploads:/app/uploads
    restart: unless-stopped
    networks:
      - yh-ai-ppt

  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
      - ./ssl:/etc/nginx/ssl
      - ./uploads:/var/www/uploads
    depends_on:
      - frontend
      - backend
    restart: unless-stopped
    networks:
      - yh-ai-ppt

networks:
  yh-ai-ppt:
    driver: bridge
```

## 环境变量配置

### 生产环境变量 (`.env`)

```env
# ==================== 基础配置 ====================
NODE_ENV=production
PORT=1111

# ==================== 数据库 ====================
DATABASE_URL="file:./prisma/prod.db"

# ==================== JWT ====================
JWT_SECRET="your-strong-secret-key-min-32-chars"
JWT_EXPIRES_IN="7d"

# ==================== AI 模型 ====================
GEMINI_API_KEY="your-production-gemini-key"
GEMINI_BASE_URL="https://generativelanguage.googleapis.com"

# 火山引擎（国内生图）
VOLCENGINE_API_KEY="your-volcengine-key"
VOLCENGINE_BASE_URL="https://ark.cn-beijing.volces.com"

# ==================== 支付配置 ====================
# 支付宝
ALIPAY_MOCK_MODE=false
ALIPAY_APP_ID="your-alipay-app-id"
ALIPAY_PRIVATE_KEY="your-private-key"
ALIPAY_PUBLIC_KEY="your-public-key"

# 微信支付
WECHAT_MOCK_MODE=false
WECHAT_MCH_ID="your-mch-id"
WECHAT_APP_ID="your-app-id"
WECHAT_API_KEY="your-api-key"
WECHAT_CERT_PATH="/app/certs/wechat.pem"

# ==================== 邮件服务 ====================
SMTP_HOST="smtp.example.com"
SMTP_PORT=587
SMTP_USER="noreply@example.com"
SMTP_PASS="your-smtp-password"

# ==================== 存储配置 ====================
UPLOAD_DIR="/app/uploads"
MAX_FILE_SIZE=52428800

# ==================== 日志 ====================
LOG_LEVEL="info"
LOG_FILE="/app/logs/app.log"
```

## SSL 证书配置

### 使用 Let's Encrypt（免费）

```bash
# 安装 Certbot
sudo apt install certbot python3-certbot-nginx

# 申请证书
sudo certbot --nginx -d your-domain.com

# 自动续期
sudo certbot renew --dry-run
```

### 手动配置证书

```bash
# 上传证书到服务器
scp fullchain.pem privkey.pem root@your-server:/etc/nginx/ssl/

# 设置权限
sudo chmod 600 /etc/nginx/ssl/privkey.pem
sudo chmod 644 /etc/nginx/ssl/fullchain.pem
```

## 备份策略

### 自动备份脚本

```bash
#!/bin/bash
# backup.sh

BACKUP_DIR="/backups/yh-ai-ppt"
DATE=$(date +%Y%m%d_%H%M%S)

# 创建备份目录
mkdir -p $BACKUP_DIR

# 备份数据库
cp /var/www/yh-ai-ppt/server/prisma/prod.db $BACKUP_DIR/prod_$DATE.db

# 备份上传文件
tar -czf $BACKUP_DIR/uploads_$DATE.tar.gz /var/www/yh-ai-ppt/uploads/

# 保留最近30天备份
find $BACKUP_DIR -name "*.db" -mtime +30 -delete
find $BACKUP_DIR -name "*.tar.gz" -mtime +30 -delete

echo "Backup completed: $DATE"
```

添加到定时任务:
```bash
# 每天凌晨3点执行
0 3 * * * /var/www/yh-ai-ppt/scripts/backup.sh >> /var/log/yh-ai-ppt-backup.log 2>&1
```

## 监控与日志

### 日志收集

```bash
# 查看应用日志
docker-compose logs -f backend

# 查看 Nginx 访问日志
docker-compose logs -f nginx

# 查看错误日志
tail -f /var/log/yh-ai-ppt/error.log
```

### 健康检查

```bash
# API 健康检查
curl https://your-domain.com/api/health

# 前端检查
curl -I https://your-domain.com
```

## 更新部署

### 零停机更新

```bash
# 1. 拉取最新代码
git pull origin main

# 2. 重新构建
docker-compose build --no-cache

# 3. 滚动更新
docker-compose up -d --remove-orphans

# 4. 清理旧镜像
docker image prune -f
```

### 数据库迁移

```bash
# 备份数据库
cp server/prisma/prod.db server/prisma/prod.db.backup

# 执行迁移
docker-compose exec backend npx prisma migrate deploy

# 验证
docker-compose exec backend npx prisma validate
```

## 故障排查

### 服务无法启动

```bash
# 查看日志
docker-compose logs backend

# 检查端口占用
sudo netstat -tlnp | grep 1111

# 检查磁盘空间
df -h
```

### 数据库连接失败

```bash
# 检查文件权限
ls -la server/prisma/

# 检查数据库文件
sqlite3 server/prisma/prod.db ".tables"
```

### 性能问题

```bash
# 查看资源使用
docker stats

# 查看 Nginx 连接数
sudo netstat -an | grep :80 | wc -l
```

---

**文档版本**: 1.0.0  
**最后更新**: 2026-02-16

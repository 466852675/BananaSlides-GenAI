# 🚀 YH-AI PPT 测试账号清单

为了方便开发与验收，系统已统一重塑了测试账号体系。所有账号的默认密码均为 `Test123456!`。

## 1. 账号列表 (Account List)

| 权限角色 | 登录标识 (Identity) | 用户名 (Username) | 备注 |
| :--- | :--- | :--- | :--- |
| **超级管理员** | `admin@bananaslides.com` | `admin` | 系统最高权限，进入 `/admin` |
| **超级管理员** | `admin01@local` | `admin01` | 备用超管账号，进入 `/admin` |
| **管理员** | `admin123@bananaslides.com` | `admin123` | 业务管理权限，进入 `/admin` |
| **普通用户** | `466852675@qq.com` | `user466` | 标准用户权限，进入 `/dashboard` |
| **手机用户** | `13294120521` | - | 手机号快捷登录测试 |

---

## 2. 登录说明 (Login Instructions)

-   **全平台通用密码**: `Test123456!`
-   **账号登录**: 标识符支持输入 **电子邮箱** 或 **用户名**。
-   **手机登录**:
    -   使用手机号 `13294120521`。
    -   验证码默认为 `123456` (Mock 模式) 或查看后端终端输出。
-   **跳转规则**:
    -   `SUPER_ADMIN` / `ADMIN` -> 自动重定向至 **管理后台 (`/admin`)**。
    -   `USER` -> 自动重定向至 **用户工作台 (`/dashboard`)**。

---

> **注意**: 本文档由系统自动生成。如需重置这些账号，请在 `server` 目录下运行 `npx ts-node prisma/seed_users.ts`。

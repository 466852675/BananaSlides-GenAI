# 图片生成技术细节

## 概述

图片生成是 YH-AI PPT 的**核心AI能力**，负责将幻灯片文本内容和视觉风格配置转换为高质量的PPT页面图片。系统采用**4层Prompt智能合成算法**，结合Vision模型的视觉分析能力，实现"风格基因"的精准复制。

## 核心功能特性

| 功能特性 | 说明 | 技术亮点 |
|---------|------|---------|
| 4层Prompt合成 | L1视觉基因+L2业务语义+L3指令合成+L4技术规格 | 确保风格一致性 |
| Vision预解析 | 使用Vision模型分析参考图提取风格关键词 | 风格精准复刻 |
| 智能风格匹配 | 精确匹配/降级匹配/无参考三级策略 | 灵活适配 |
| 分辨率自适应 | 支持1K/2K/4K，自动32像素对齐 | 高清输出 |
| Seed锁定 | 火山引擎支持Seed参数锁定生成结果 | 结果可复现 |
| 多厂商适配 | Gemini/Volcengine/智谱等差异化处理 | 统一接口 |

---

## 系统架构

### 1. 数据流架构

```
┌─────────────────────────────────────────────────────────────┐
│                        图片生成流程                          │
├─────────────────────────────────────────────────────────────┤
│  1. 输入层                                                  │
│     ├── 页面内容 (标题/正文/设计建议)                        │
│     ├── 风格配置 (配色/字体/整体风格)                        │
│     ├── 页面类型 (封面/目录/内容/结束)                       │
│     └── 参考图Map (globalStyleMap)                          │
│                          ↓                                  │
│  2. Vision分析层                                            │
│     ├── 智能匹配最佳风格参考图                               │
│     └── Vision模型提取风格关键词 (颜色/构图/肌理)            │
│                          ↓                                  │
│  3. Prompt构建层 (4层合成)                                  │
│     ├── L1: 视觉基因 (Vision提取的风格特征)                  │
│     ├── L2: 业务语义 (页面标题和内容)                        │
│     ├── L3: 指令合成 (如何融合风格与内容)                    │
│     └── L4: 技术规格 (分辨率/比例/画质)                      │
│                          ↓                                  │
│  4. 图片生成层                                              │
│     ├── 协议适配 (Gemini Native / OpenAI Compatible)        │
│     ├── 分辨率计算 (按厂商优化)                              │
│     └── 图片保存与URL返回                                   │
└─────────────────────────────────────────────────────────────┘
```

### 2. 核心组件关系

```typescript
// 前端调用
const result = await fetch('/api/ai/generate-slide-variant', {
  method: 'POST',
  body: JSON.stringify({
    contentSource: slide.content,
    styleFile: template.styleImage,
    configStyle: {
      styleName: "科技蓝",
      colorPalette: "#0A192B,#64FFDA,#8892B0",
      requirements: "总体视觉规范...",
      aspectRatio: "16:9"
    },
    pageType: "content",
    fullContent: slide.content + "\n---DESIGN_SUGGESTION_START---\n**设计建议：** 采用左右分栏..."
  })
});
```

---

## 核心算法实现

### 1. Vision风格分析

```typescript
/**
 * 视觉特征提取: 使用 Vision 模型分析风格图并返回关键词
 */
const analyzeStyleImage = async (
    imageResource: string,
    pageType: string,
    settings?: AppSettings
): Promise<string> => {
    const config = await resolveActiveConfig(settings, 'vision');
    const base64 = await resourceToBase64(imageResource);

    // Vision Prompt：提取抽象设计属性
    const prompt = `
        Role: 顶级 PPT 视觉分析师 & 构图专家。
        Task: 解构这张 PPT ${pageType} 的"视觉元属性"，为图像生成模型重建其灵魂。
        
        重点提取以下抽象设计属性 (禁止描述具象物品，禁止提取文字内容):
        1. 【主色调 - 最重要】: 必须明确指出画面的主导颜色名称（如：荧光绿、赛博蓝、琥珀金、科技紫等），以及辅助色。这是风格复现的核心！
        2. 【构图骨架】: 描述画面的几何重心分配（如：偏左重心、居中对称、大块非对称切割）。
        3. 【空间层级】: 识别其视觉深度（如：多层 3D 堆叠、极简扁平层级、半透明重叠感）。
        4. 【背景肌理】: 描述背景是某种纹理（如：电路板肌理、流体渐变、深色纯色）以及背景颜色。
        5. 【发光效果】: 是否有霓虹光晕、扫描线、粒子效果等科技感元素。
        
        输出要求:
        - 语言: 简体中文。
        - 格式: 极简扫描指令，必须首先明确主色调。
        - 示例: "主色调为荧光绿配深黑背景，构图为...，视觉采用电路板肌理，配合绿色霓虹光晕效果"
        - 长度: 150 字以内。
    `;

    if (shouldUseGeminiNative(config, settings)) {
        const ai = createGoogleClient(config);
        const contents = [
            { inlineData: { mimeType: 'image/png', data: base64 } },
            { text: prompt }
        ];
        const response = await ai.models.generateContent({
            model: config.model,
            contents: { parts: contents } as any
        });
        return response.text?.trim() || "";
    } else {
        // OpenAI Compatible Vision API
        const messages = [
            {
                role: "user",
                content: [
                    { type: "text", text: prompt },
                    { type: "image_url", image_url: { url: `data:image/png;base64,${base64}` } }
                ]
            }
        ];
        return await callOpenAICompatible(config, messages, 0.7, false, 'text');
    }
};
```

### 2. 智能风格参考图匹配

```typescript
/**
 * 智能匹配风格参考图
 * 三级匹配策略：精确匹配 → 降级匹配 → 无参考
 */
const getStyleReference = (
    pageType: string,
    globalStyleMap?: GlobalStyleMap
): { file: string | null; matchType: 'exact' | 'fallback' | 'none' } => {
    if (!globalStyleMap) return { file: null, matchType: 'none' };

    // Level 1: 精确匹配当前页面类型
    const exactMatch = globalStyleMap[pageType as PageType];
    if (exactMatch) {
        return { file: exactMatch, matchType: 'exact' };
    }

    // Level 2: 降级到其他页面类型的风格参考图
    const otherTypes: PageType[] = ['cover', 'content', 'directory', 'transition', 'end'];
    for (const type of otherTypes) {
        if (type !== pageType && globalStyleMap[type]) {
            return { file: globalStyleMap[type], matchType: 'fallback' };
        }
    }

    return { file: null, matchType: 'none' };
};
```

### 3. 4层Prompt智能合成

```typescript
/**
 * 构建完整的图片生成Prompt (OpenAI兼容模式)
 * 4层结构：视觉语言 → 业务内容 → 合成指令 → 技术规格
 */
const buildImageGenerationPrompt = (params: {
    pageType: string;
    title: string;
    content: string;
    styleName: string;
    colorPalette: string;
    requirements: string;
    aspectRatio: string;
    styleMatchType: 'exact' | 'fallback' | 'none';
    allSlideTitles?: string[];
    styleKeywords?: string;
    designSuggestion?: string;
    isZhipu?: boolean;
}): string => {
    const { pageType, title, content, styleKeywords, designSuggestion, isZhipu } = params;

    let prompt = '';

    // ============================================================
    // 🚨 核心指令：严禁显示技术规格
    // ============================================================
    if (isZhipu) {
        prompt += `【🚨 重要指令】图片中严禁出现任何颜色代码、字体名称、宽高比等技术参数。文字结尾严禁使用标点符号。图片中只能出现【业务内容】部分的标题和正文！\n\n`;
    } else {
        prompt += `【🚨 最高优先级指令 - 内容隔离规则】\n`;
        prompt += `以下规则必须严格遵守，否则视为生成失败：\n`;
        prompt += `1. [禁止渲染区] 本 Prompt 中的所有"技术规格"和"风格参数"仅供您内部处理参考，严禁在图片中渲染！\n`;
        prompt += `2. [禁止渲染清单] 以下内容绝对不能出现在生成的图片中：\n`;
        prompt += `   - 任何颜色代码（如 #0052D4, #1C1C1, #0A192B, RGB值等）\n`;
        prompt += `   - 任何字体名称（如 思源体, Arial, 微软雅黑等）\n`;
        prompt += `   - 任何宽高比标注（如 16:9, 4:3, 1:1等）\n`;
        prompt += `   - 任何调色板名称（如 琥珀金, 冷色调, 渐变等）\n`;
        prompt += `3. [PPT标点规范] PPT不是文档，文字结尾严禁使用中文句末标点：。；，！？、\n`;
        prompt += `4. [唯一可渲染内容] 图片中只能出现【业务任务内容】部分的标题和正文！\n\n`;
    }

    // ============================================================
    // L1: 视觉语言 & 艺术风格 (Visual Language - "HOW to draw")
    // ============================================================
    prompt += `【1. 视觉语言 & 艺术风格 (仅供内部参考，禁止渲染)】\n`;
    if (styleKeywords) {
        prompt += `- [最重要] 核心视觉指纹 (必须严格遵循): ${styleKeywords}\n`;
        prompt += `- [颜色约束] 上述指纹中的主色调是风格的灵魂，必须作为画面的主导颜色！\n`;
        prompt += `- [背景统一 (禁止白头)]: 页面必须使用【全屏沉浸式背景】。严禁在顶部出现白色的标题栏！\n`;
    }

    // 用户明确的设计建议
    if (designSuggestion) {
        prompt += `- [用户设计建议 (必须采纳)]: ${designSuggestion}\n`;
        prompt += `- [可视化强制]: 如果建议中包含图表形式（如环形图、蜂窝图），必须作为核心视觉主体！\n`;
    }

    // ============================================================
    // L2: 业务任务核心 (Business Core - "WHAT to draw")
    // ============================================================
    prompt += `\n【2. 业务任务内容 (✅ 这是唯一可渲染的内容)】\n`;
    prompt += `- 页面标题: "${title}"\n`;

    if (content) {
        const contentPreview = content.length > 800 ? content.substring(0, 800) + '...' : content;
        prompt += `- 详细业务描述: "${contentPreview}"\n`;

        // 内容页特殊优化
        if (pageType === 'content' && !isZhipu) {
            prompt += `\n【🌟 内容页排版增强指令】\n`;
            prompt += `- [文案适度丰富]: 增加信息密度，通过多层级结构展示内容丰富度。\n`;
            prompt += `- [智能提炼]: 每个核心板块展示 1-2 行简短说明（约15-20字）。\n`;
            prompt += `- [视觉层级]: 核心模块="主图标+标题+关键句+数据展示"。\n`;
            prompt += `- [装饰细节]: 每个模块增加 2-3 个装饰性小标签、状态胶囊或数据角标。\n`;
            prompt += `- [核心金句]: 在画面留白处展示一句从业务描述中提炼的核心观点（不超过15字）。\n`;
        }
    }

    // ============================================================
    // L3: 合成指令 (Synthesized Meta-Instruction)
    // ============================================================
    prompt += `\n【3. 合成指令】\n`;
    prompt += `- 任务使命: 使用第一部分的【视觉语言】风格渲染第二部分的【业务任务内容】。\n`;
    prompt += `- [CRITICAL] 再次强调：颜色代码、字体名称、比例数字等技术参数严禁出现在画面中！\n`;

    // ============================================================
    // L4: 技术规格 (Technical Specs - 内部参数)
    // ============================================================
    prompt += `\n【4. 技术规格 (内部处理参数，禁止渲染到画面)】\n`;
    prompt += `- 输出宽高比: ${params.aspectRatio} (仅控制画布比例)\n`;
    prompt += `- 画面基调: 专业商业演示, 4K 高画质, 文字清晰。\n`;

    return prompt;
};
```

### 4. 分辨率计算与适配

```typescript
/**
 * 计算最终分辨率
 * 支持不同厂商的特殊分辨率要求
 */
const calculateFinalResolution = (
    qualitySetting: string | undefined,
    aspectRatio: string,
    isZhipu: boolean = false
): string => {
    let tier = '4K'; // 默认4K
    if (qualitySetting?.includes('1024') || qualitySetting?.includes('1280')) tier = '1K';
    else if (qualitySetting?.includes('2048') || qualitySetting?.includes('1920')) tier = '2K';

    const ratio = getClosestSupportedRatio(aspectRatio);

    // 智谱AI特殊分辨率 (CogView模型)
    const zhipuMap: Record<string, string> = {
        '16:9': '1728x960',
        '4:3': '1344x1024',
        '1:1': '1280x1280',
        '3:4': '1024x1344',
        '9:16': '960x1728'
    };

    if (isZhipu) {
        return zhipuMap[ratio] || '1280x1280';
    }

    // 标准分辨率映射
    const map: Record<string, Record<string, string>> = {
        '1K': {
            '16:9': '1280x736',
            '4:3': '1024x768',
            '1:1': '1024x1024',
            '3:4': '768x1024',
            '9:16': '736x1280'
        },
        '2K': {
            '16:9': '1920x1088',
            '4:3': '2048x1536',
            '1:1': '2048x2048',
            '3:4': '1536x2048',
            '9:16': '1088x1920'
        },
        '4K': {
            '16:9': '3840x2176',
            '4:3': '3072x2304',
            '1:1': '2176x2176',
            '3:4': '2304x3072',
            '9:16': '2176x3840'
        }
    };

    let size = map[tier][ratio] || map['2K']['16:9'];

    // 确保 W/H 是 32 的倍数 (针对所有非原生模型)
    let [finalW, finalH] = size.split('x').map(Number);
    finalW = Math.round(finalW / 32) * 32;
    finalH = Math.round(finalH / 32) * 32;
    return `${finalW}x${finalH}`;
};
```

---

## API 接口

### 生成幻灯片变体

**Endpoint:** `POST /api/ai/generate-slide-variant`

**Request Body:**
```typescript
{
  contentSource: string;        // 内容来源 (文本或图片URL)
  styleFile: string | null;     // 风格参考图路径
  configStyle: {
    styleName: string;          // 风格名称
    colorPalette: string;       // 配色方案
    requirements: string;       // 视觉规范
    aspectRatio: string;        // 宽高比 (16:9/4:3/1:1/9:16)
  };
  variantLabel: string;         // 变体标签
  title: string;                // 页面标题
  contentType: 'text' | 'image'; // 内容类型
  pageType?: string;            // 页面类型
  fullContent?: string;         // 完整内容(含设计建议)
  globalStyleMap?: {            // 全局风格图Map
    cover?: string;
    directory?: string;
    transition?: string;
    content?: string;
    end?: string;
  };
}
```

**Response:**
```typescript
{
  success: true;
  data: string;  // 生成的图片URL
  pointsDeducted: number;
}
```

---

## 页面类型差异处理

### 1. 封面页 (cover)

```typescript
const getPageTypeInstructions = (pageType: string): string => {
    const commonRules = '【必须遵守】: 页面中的标题名称必须去重,保证生成的页面中只有唯一的一个标题。';

    const instructions: Record<string, string> = {
        cover: `生成专业的PPT封面页。要求:标题突出醒目,背景视觉大气。标题必须简洁有力，不得包含副标题。${commonRules}`,
        
        directory: `生成清晰的PPT目录页。要求:1)章节列表条理清晰,层级分明;2)根据提供的内容智能归纳目录结构;3)严禁偏离主题。${commonRules}`,
        
        transition: `生成章节过渡页。要求:突出显示章节标题,具有承上启下的视觉过渡效果。标题为"章节标题"而非"章节过渡"。${commonRules}`,
        
        content: `生成内容展示页。要求:1)在标题下方添加一句核心总结语句;2)内容需要有层次地展示:提炼小标题、关键要素、视觉标签;3)避免大段文字堆叠。${commonRules}`,
        
        end: `生成PPT结束页。要求:严格根据提供的标题和描述生成,严禁包含联系电话、电子邮箱、网址、地址等占位符信息。${commonRules}`
    };
    
    return instructions[pageType] || instructions.custom;
};
```

---

## 厂商特殊处理

### 1. 火山引擎 (Volcengine)

```typescript
// Seed锁定实现
const isVolcengine = config.baseUrl.toLowerCase().includes('volces.com');

if (isVolcengine) {
    // 基于Prompt内容生成确定性Seed
    const promptHash = prompt.substring(0, 200).split('').reduce((acc, char) => {
        return ((acc << 5) - acc) + char.charCodeAt(0) | 0;
    }, 0);
    const seed = Math.abs(promptHash) % 2147483647;
    
    body.seed = seed;
    body.sequential_image_generation = "disabled";
    body.watermark = false;
    body.stream = false;
    
    // 支持风格参考图 (图生图)
    if (styleImageUrl) {
        body.image = styleImageUrl;
    }
}
```

### 2. 智谱AI (Zhipu)

```typescript
const isZhipu = config.baseUrl.toLowerCase().includes('bigmodel.cn') ||
    config.model.toLowerCase().includes('glm') ||
    config.model.toLowerCase().includes('cogview');

if (isZhipu) {
    body.quality = "hd"; // CogView-3-plus 推荐
    body.watermark_enabled = false;
    body.watermark = false;
    body.has_watermark = false;
}
```

---

## 积分扣费

图片生成消耗 **5积分** (VIP用户享受折扣):

| 用户类型 | 积分消耗 | 说明 |
|---------|---------|------|
| 普通用户 | 5 | 标准价格 |
| VIP 1-2级 | 4 | 8折 |
| VIP 3-4级 | 3 | 6折 |
| VIP 5级 | 0-2 | 免费或超低折扣 |

---

## 错误处理

| 错误场景 | 处理策略 |
|---------|---------|
| Vision分析失败 | 降级为仅使用Prompt生成 |
| 风格参考图不存在 | 使用无参考模式生成 |
| 图片生成超时 | 3分钟超时，指数退避重试 |
| 厂商API限流 | 自动重试，最多5次 |
| 生成结果不合规 | 返回错误，不退还积分 |

---

## 扩展阅读

- [多模型路由](./多模型路由.md) - AI服务提供商配置
- [模板概念分析](./模板概念分析.md) - 风格配置详解
- [幻灯片正文生成](./幻灯片正文生成.md) - 内容生成流程

---

## 变更历史

| 版本 | 日期 | 变更内容 |
|------|------|---------|
| v1.0 | 2026-02-16 | 基于代码实现重写，添加4层Prompt架构 |
| v1.1 | - | 添加Vision预解析功能 |
| v1.2 | - | 添加智谱/火山引擎特殊处理 |

---

**维护者:** YH-AI PPT 技术团队  
**最后更新:** 2026-02-16

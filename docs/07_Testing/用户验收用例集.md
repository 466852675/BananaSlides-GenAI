# 🚀 YH-AI PPT 终极全维验收测试 (UAT) 手册 (Ultimate V3.1)

**版本**: V3.1 (与PRD v2.0对齐)  
**最后更新**: 2026-02-16  
**覆盖维度**: 功能 / 数据 / 安全 / 性能 / 兼容性 / 国际化 / API 契约 / 管理后台  
**测试工具**: Chrome DevTools, Postman (可选), Sqlite3 CLI  
**PRD v2.0对齐**: 覆盖36个功能模块、196+接口、28个数据实体

---

## 🧪 1. 仪表盘与可视化 (Dashboard & Analytics)

| ID | 测试场景 | 操作步骤 (Step-by-Step) | 预期结果 (UI) | 技术验证 (DevTools/DB) |
|:---|:---|:---|:---|:---|
| **DB-001** | **空状态 (Zero State)** | 1. 清空数据库 `npm run db:init`。<br>2. 访问首页。 | 显示"开启您的第一个项目"引导页。统计栏均为 0。 | API `/projects` 返回空数组 `[]`。 |
| **DB-002** | **统计计算逻辑** | 1. 确保 DB 有 3 个项目 (2完成, 1进行中)。<br>2. 刷新页面。 | 统计栏显示: Total=3, Completed=2, Active=1。 | 核对前端 `useMemo` 计算结果与 DB 记录一致。 |
| **DB-003** | **跑马灯交互** | 1. 点击顶部跑马灯 "⚡ 正在进行中: Project X"。 | 页面无刷新跳转至 `Project X` 的 Workbench。 | URL 变为 `/project/:id`，且 React Router 状态更新。 |
| **DB-004** | **响应式布局 (Mobile)** | 1. F12 切换至 iPhone SE 视图。<br>2. 观察统计栏。 | 统计栏应变为**水平滚动**容器，不应换行错乱。 | CSS `overflow-x-auto` 生效。 |
| **DB-005** | **筛选器组合逻辑** | 1. 状态选 "生成中"。<br>2. 搜索框输入 "不存在的标题"。 | 列表显示为空 (Empty State)。 | 过滤逻辑应为 `AND` 关系。 |

## 🧪 2. 项目生命周期 (Lifecycle Management)

| ID | 测试场景 | 操作步骤 | 预期结果 | 异常/边界 |
|:---|:---|:---|:---|:---|
| **PRJ-001** | **标题长度边界** | 创建项目时输入 100 个 "A"。 | 标题应在卡片上截断显示 (省略号)，不破坏布局。 | DB 字段应能完整存储。 |
| **PRJ-002** | **状态流转: Generating** | 1. 点击生成。<br>2. 迅速返回 Dashboard。 | 卡片状态徽章变为蓝色 `生成中` (Pulse 动画)。 | 此时禁止删除该项目 (按钮禁用)。 |
| **PRJ-003** | **状态流转: Completed** | 1. 模拟所有 Slide 生成完毕。<br>2. 观察卡片。 | 进度条变绿 (100%)，徽章变为绿色 `已完成`。 | 项目自动移出 "Active" 视图 (除非筛选器选 All)。 |
| **PRJ-004** | **物理删除验证** | 1. 删除一个含图片的项目。<br>2. 检查后端。 | UI 卡片消失。 | **(待优化)** 检查 `server/uploads` 目录，确认图片文件是否被标记清理。 |

## 🧪 3. 创作室 - 输入与大纲 (Workbench Input)

| ID | 测试场景 | 操作步骤 | 关键验证点 | API 契约验证 |
|:---|:---|:---|:---|:---|
| **INP-001** | **MinerU PDF 解析** | 1. 配置 MinerU Key。<br>2. 上传包含公式的 PDF。 | UI 显示 "调用 MinerU 解析中..."。 | Network 请求 `/api/doc-parser/parse`，Payload 包含 File。 |
| **INP-002** | **解析降级 (Fallback)** | 1. 故意填错 MinerU Key。<br>2. 上传 PDF。 | UI Toast 提示 "MinerU 失败，切换至 Vision"。 | 后端日志显示 `Fallback to Vision Model`。 |
| **INP-003** | **以图配文流程** | 1. 上传本地图片 `demo.png`。<br>2. 检查生成的大纲。 | Slide 类型自动设为 `Image`，内容为空或 AI 自动补全的描述。 | `contentType` 字段为 `image`。 |
| **OUT-001** | **大纲结构完整性** | 生成大纲。 | 必须包含 `Cover`, `Directory`, `Content`, `End` 四种类型。 | JSON 响应中 `pageType` 字段枚举值正确。 |
| **OUT-002** | **拖拽排序稳定性** | 快速拖动第 2 页到第 5 页，再拖回来。 | 序号 (Index) 实时重算，无跳变。 | `index` 字段连续，无重复。 |

## 🧪 4. 核心生成引擎 (Generation Engine)

| ID | 测试场景 | 操作步骤 | 预期结果 | 并发验证 |
|:---|:---|:---|:---|:---|
| **GEN-001** | **并发队列 (Queue)** | 1. 设置 `ImageConcurrency=1`。<br>2. 批量生成 5 页。 | UI 上同一时间只有 **1 个** Loading 转圈。 | Network 面板中 Pending 请求数不超过 1。 |
| **GEN-002** | **Smart Refine** | 选中 "画个猫" -> 点击魔法棒。 | 文本变为 "写实风格，布偶猫，窗台阳光..."。 | 调用 `/api/ai/smart-refine` 接口。 |
| **GEN-003** | **变体生成 (Variants)** | 1. 点击生成单页。<br>2. 等待完成。 | 右侧出现 2-4 张缩略图。默认选中第 1 张。 | DB 中 `variants` 字段存储为 JSON Array。 |
| **GEN-004** | **变体热切换** | 点击变体 2。 | 左侧主图**瞬间**切换 (无 Loading)。 | 纯前端状态切换，无网络请求。 |
| **GEN-005** | **错误重试** | 1. 断网导致生成失败 (红色)。<br>2. 联网后点击 "重试"。 | 状态重置为 Loading，随后成功。 | 状态机从 `Error` -> `Generating` -> `Success`。 |

## 🧪 5. 全局资源与资产 (Resources)

| ID | 测试场景 | 操作步骤 | 预期结果 | 数据一致性 |
|:---|:---|:---|:---|:---|
| **TMP-001** | **模板创建 (复杂)** | 1. 创建模板。<br>2. 上传 5 张不同类型的参考图。<br>3. 设置 Prompt。<br>4. 保存。 | 模板保存成功。再次编辑时所有图片和文字均还原。 | `StyleTemplate` 表中 `styleMap` JSON 完整。 |
| **TMP-002** | **跨项目引用** | 1. 在项目 B 中应用模板 A。<br>2. 生成图片。 | AI 绘图时参考了模板 A 中的图片风格。 | Prompt 中包含 Image Reference 参数。 |
| **FAV-001** | **收藏夹同步** | 1. 收藏当前风格。<br>2. 修改名称。<br>3. 刷新页面。 | 名称修改已持久化。 | `Favorite` 表记录更新。 |

## 🧪 6. 历史版本与时光机 (History)

| ID | 测试场景 | 操作步骤 | 预期结果 | 风险控制 |
|:---|:---|:---|:---|:---|
| **HIS-001** | **AI 摘要生成** | 保存快照。 | 几秒后，摘要从 "Computing..." 变为 "Updated title..."。 | 若 AI 失败，摘要应回退为 "手动保存"。 |
| **HIS-002** | **只读模式 (Lock)** | 进入历史预览。 | 所有输入框 (`input`, `textarea`) 均变为 `disabled`。 | 防止用户误以为在编辑当前项目。 |
| **HIS-003** | **全量回滚** | 点击 "恢复此版本"。 | 当前项目的 Title, Config, Slides 全部被快照覆盖。 | **高危**: 确认回滚前的未保存修改已被提示覆盖。 |

## 🧪 7. 国际化与本地化 (i18n & L10n)

| ID | 测试场景 | 操作步骤 | 预期结果 |
|:---|:---|:---|:---|
| **INT-001** | **切换语言** | 全局设置 -> 语言 -> English。 | 1. 界面主要按钮变为英文。<br>2. AI 生成的 Prompt 指令追加 "Output in English"。 |
| **INT-002** | **日语支持** | 全局设置 -> 语言 -> 日本語。 | AI 生成的内容 (大纲/文案) 变为日文。 |

## 🧪 8. 性能基准测试 (Benchmarks)

| ID | 测试场景 | 达标标准 | 验证方法 |
|:---|:---|:---|:---|
| **PERF-001** | **首屏加载时间 (LCP)** | < 1.0 秒 (Localhost) | Chrome Lighthouse |
| **PERF-002** | **大纲生成延迟** | < 10 秒 (Gemini Pro) | 计时器 |
| **PERF-003** | **图片生成延迟** | < 15 秒 (DALL-E 3) | 计时器 |
| **PERF-004** | **高并发响应** | 同时点开 3 个项目生成 | UI 不卡顿，FPS > 30。 | Performance Monitor |

## 🧪 9. 数据持久化 (Data Persistence)

| ID | 测试场景 | 操作步骤 | 预期结果 | 灾难恢复 |
|:---|:---|:---|:---|:---|
| **PST-001** | **Flush-on-Exit** | 修改标题 -> 不点保存 -> 直接点浏览器后退。 | 再次进入，标题已保存。 | 验证 `useEffect` 卸载钩子生效。 |
| **PST-002** | **服务崩溃重启** | `Kill` 后端进程 -> 重启 -> 刷新前端。 | 所有项目数据完好无损。 | 验证 SQLite 文件未损坏。 |
| **PST-003** | **浏览器崩溃** | 编辑中途强制关闭 Tab。 | 丢失数据不超过 400ms (Debounce 时间)。 | N/A |

## 🧪 10. 安全与防御 (Security)

| ID | 测试场景 | 攻击模拟 | 防御表现 |
|:---|:---|:---|:---|
| **SEC-001** | **XSS 注入** | 输入 `<img src=x onerror=alert(1)>` | 文本原样显示，无弹窗。 | React 默认转义机制生效。 |
| **SEC-002** | **RCE 上传防御** | 上传 `hack.php` | 后端返回 `400 Bad Request`，文件未落地。 | `uploadMiddleware` 白名单生效。 |
| **SEC-003** | **API Key 泄露** | 查看 `/api/settings` 响应 | Key 显示为 `sk-****` 或加密串。 | 后端 Mask 处理生效。 |

---

## 📊 验收记分卡

| 模块 | 总用例数 | 通过数 | 阻断级 Bug | 严重级 Bug |
|:---|:---:|:---:|:---:|:---:|
| 仪表盘 | 5 | | | |
| 创作室 | 8 | | | |
| 生成引擎 | 5 | | | |
| 资源体系 | 3 | | | |
| 历史版本 | 3 | | | |
| 性能与安全 | 7 | | | |
| **合计** | **31** | | | |

**验收结论**:
*   [ ] **Ready for Release**: 所有关键路径通过，无 P0/P1 Bug。
*   [ ] **Fix Required**: 存在阻断性问题，需修复后重测。

**测试工程师**: ____________________  
**产品经理**: ____________________  
**日期**: 2026-01-17
